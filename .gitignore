node_modules/
dist/
*.tsbuildinfo

# Recorded sessions contain LIVE cookies and session tokens — never commit them.
.recording/

# Claude Code local hooks, logs and internal state — not part of the project.
.claude/
*.log

